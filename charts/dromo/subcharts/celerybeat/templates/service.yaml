apiVersion: v1
kind: Service
metadata:
  name: {{ include "celerybeat.fullname" . }}
  labels:
    {{- include "celerybeat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "celerybeat.selectorLabels" . | nindent 4 }}
